import React from 'react';
import { useParams } from 'react-router-dom';

const Register = (props) => {
    const {title, img, _id} = props.vservice;
    const {id} = useParams();
    return (
        <div>
            
        </div>
    );
};

export default Register;